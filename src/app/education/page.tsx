import Education from '@/components/education';
import SectionWrapper from '@/components/section-wrapper';

export default function EducationPage() {
  return (
    <SectionWrapper id="education" className="pt-20">
      <Education />
    </SectionWrapper>
  );
}
